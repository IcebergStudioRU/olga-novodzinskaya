import React from "react";

const Object = ({
  id,
  url,
  title,
  description,
  cost,
  rooms,
  floor,
  totalFloors,
  squareMeters,
  photos,
  place,
  district,
  address,
}) => {
  return <div>{title}</div>;
};

export default Object;
