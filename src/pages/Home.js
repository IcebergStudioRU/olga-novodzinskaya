import React from "react";
import Header from "../components/Header";
import Screen from "../components/Screen";

const Home = () => {
  return (
    <Screen>
      <Header />
    </Screen>
  );
};

export default Home;
